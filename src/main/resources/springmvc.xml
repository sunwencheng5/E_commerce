<?xml version="1.0" encoding="UTF-8"?>
<beans	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xmlns="http://www.springframework.org/schema/beans"
          xmlns:context="http://www.springframework.org/schema/context"
          xmlns:mvc="http://www.springframework.org/schema/mvc"
          xsi:schemaLocation="http://www.springframework.org/schema/beans
	http://www.springframework.org/schema/beans/spring-beans-4.2.xsd
    http://www.springframework.org/schema/context
	http://www.springframework.org/schema/context/spring-context-4.2.xsd
    http://www.springframework.org/schema/mvc
	http://www.springframework.org/schema/mvc/spring-mvc-4.2.xsd">

    <!-- 配置扫描，扫描@Controller注解的类 -->
    <context:component-scan base-package="com.lwzy.controller"></context:component-scan>
    <!-- 配置注解驱动 -->
    <mvc:annotation-driven />
    <!-- 允许static目录下所有文件可见 -->
    <mvc:resources location="/static/" mapping="/static/**"></mvc:resources>
    <mvc:interceptors>
        <mvc:interceptor>
            <mvc:mapping path="/cart/**"/>
            <bean class="com.lwzy.interceptor.BuserIsLoginInterceptor"/>
        </mvc:interceptor>
        <mvc:interceptor>
            <mvc:mapping path="/goods/**"/>
            <mvc:mapping path="/goodsType/**"/>
            <mvc:mapping path="/order/**"/>
            <bean class="com.lwzy.interceptor.AdminIsLoginInterceptor"/>
        </mvc:interceptor>
    </mvc:interceptors>
    <!-- 配置视图解析器 -->
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <!--前缀-->
        <property name="prefix" value="/WEB-INF/jsp/"></property>
        <!--后缀-->
        <property name="suffix" value=".jsp"></property>
    </bean>
    <!--配置一个名为multipartResolver的bean 用于文件上传 -->
    <bean id="multipartResolver" class="org.springframework.web.multipart.support.StandardServletMultipartResolver"/>

</beans>